import React from 'react';
//components
import { Row, Col } from 'antd';

const PetCardEditModal = (props) => {
  const { data, handleFormChange, handlePetCardSubmit } = props;

  return <div className='border border-2'></div>;
};

export default PetCardEditModal;
